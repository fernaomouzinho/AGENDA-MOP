from django.urls import path
from . import views

urlpatterns = [
    #===============User===================
    #path('<str:lg>/diresaun/<slug:dir_slug>/', views.direction, name='direction'),
    #===============Admin===================

]